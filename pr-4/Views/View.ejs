<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= book.title %> | Book Details</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600&family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: 'Inter', sans-serif;
      background-color: #f5f2e8; /* warm parchment */
      color: #3b2f2f;
      line-height: 1.6;
    }

    header.main-header {
      background: #4b3f2f; /* deep brown */
      color: #f5e6c5;
      padding: 18px 0;
      border-bottom: 4px solid #c9a646;
      box-shadow: 0 6px 20px rgba(0, 0, 0, 0.15);
    }

    .main-header .container {
      max-width: 1200px;
      margin: auto;
      padding: 0 20px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .logo {
      font-family: 'Playfair Display', serif;
      font-size: 28px;
      color: #f9e7be;
      text-decoration: none;
      letter-spacing: 1px;
    }

    .nav-links a {
      text-decoration: none;
      color: #f1d7a1;
      font-weight: 600;
      margin-left: 20px;
      transition: color 0.3s ease;
    }

    .nav-links a:hover {
      color: #fff8dc;
    }

    .banner {
      background: url('https://www.transparenttextures.com/patterns/aged-paper.png'), linear-gradient(to right, #a17f56, #d8c7a0);
      color: #3b2f2f;
      text-align: center;
      padding: 70px 20px;
      border-bottom: 4px solid #c9a646;
    }

    .banner h1 {
      font-family: 'Playfair Display', serif;
      font-size: 2.8rem;
      margin-bottom: 10px;
      color: #3b2f2f;
    }

    .banner p {
      font-size: 1.15rem;
      color: #4b3f2f;
      opacity: 0.85;
    }

    .back-btn {
      display: inline-block;
      margin: 30px 0 20px 20px;
      padding: 10px 22px;
      background: #7a5c37;
      color: #fff8dc;
      border: none;
      border-radius: 6px;
      font-weight: 600;
      text-decoration: none;
      transition: background 0.3s ease, transform 0.2s ease;
      box-shadow: 0 3px 10px rgba(0, 0, 0, 0.2);
    }

    .back-btn:hover {
      background: #60492d;
      transform: translateY(-2px);
    }

    .container {
      max-width: 1100px;
      margin: 0 auto;
      padding: 20px;
    }

    .view-wrapper {
      display: flex;
      gap: 40px;
      flex-wrap: wrap;
      background: #fffef8;
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 6px 16px rgba(0, 0, 0, 0.08);
      border: 1px solid #e2d8c3;
    }

    .book-cover {
      flex: 1;
      min-width: 280px;
      max-width: 400px;
      object-fit: contain;
      border: 1px solid #c9b88a;
      padding: 10px;
      border-radius: 10px;
      background: #faf5e4;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.08);
      transition: transform 0.3s ease;
    }

    .book-cover:hover {
      transform: scale(1.03);
    }

    .details {
      flex: 2;
    }

    .details h2 {
      font-family: 'Playfair Display', serif;
      font-size: 30px;
      margin-bottom: 20px;
      color: #4b3f2f;
      border-bottom: 2px solid #c9a646;
      display: inline-block;
      padding-bottom: 6px;
    }

    .detail-row {
      margin-bottom: 12px;
    }

    .detail-label {
      font-weight: 600;
      color: #7a5c37;
    }

    .detail-value {
      color: #3b2f2f;
      margin-left: 6px;
    }

    .description {
      margin-top: 20px;
    }

    .description .detail-label {
      display: block;
      margin-bottom: 8px;
      font-size: 17px;
      font-weight: 600;
      color: #7a5c37;
    }

    .description p {
      margin: 0;
      padding: 15px;
      background: #faf5e4;
      border-left: 5px solid #c9a646;
      border-radius: 4px;
      color: #3b2f2f;
      font-size: 15px;
    }

    @media (max-width: 768px) {
      .banner h1 {
        font-size: 2.2rem;
      }

      .view-wrapper {
        flex-direction: column;
        padding: 20px;
      }

      .book-cover {
        max-width: 100%;
        margin: auto;
      }

      .details h2 {
        text-align: center;
      }

      .detail-row, .description {
        text-align: center;
      }
    }
  </style>
</head>
<body>

  <header class="main-header">
    <div class="container">
      <a href="/" class="logo">📚 The Book Haven</a>
      <div class="nav-links">
        <a href="/">Home</a>
      </div>
    </div>
  </header>

  <section class="banner">
    <div class="banner-content">
      <h1>Welcome to The Book Haven</h1>
      <p>Where every page whispers the scent of old paper and timeless tales.</p>
    </div>
  </section>

  <a href="/" class="back-btn">&larr; Back to Library</a>

  <div class="container">
    <div class="view-wrapper">
      <img src="<%= book.image %>" alt="Book Cover" class="book-cover"/>

      <div class="details">
        <h2><%= book.title %></h2>

        <div class="detail-row"><span class="detail-label">Author:</span> <span class="detail-value"><%= book.author %></span></div>
        <div class="detail-row"><span class="detail-label">Language:</span> <span class="detail-value"><%= book.language %></span></div>
        <div class="detail-row"><span class="detail-label">Genre:</span> <span class="detail-value"><%= book.genre %></span></div>
        <div class="detail-row"><span class="detail-label">Edition:</span> <span class="detail-value"><%= book.edition %></span></div>
        <div class="detail-row"><span class="detail-label">Publish Year:</span> <span class="detail-value"><%= book.publishedYear %></span></div>
        <div class="detail-row"><span class="detail-label">Price:</span> <span class="detail-value">₹<%= book.price %></span></div>

        <div class="description">
          <span class="detail-label">Description:</span>
          <p><%= book.description %></p>
        </div>
      </div>
    </div>
  </div>

</body>
</html>
