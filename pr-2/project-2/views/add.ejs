<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Add Book — Book Haven</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Poppins', sans-serif;
    }

    body {
      background: linear-gradient(135deg, #f5f7fa, #eaf4ec);
      min-height: 100vh;
      color: #333;
      display: flex;
      flex-direction: column;
    }

    /* Header */
    header {
      background: #2d6a4f;
      padding: 18px 40px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      box-shadow: 0 4px 15px rgba(0,0,0,0.1);
      position: sticky;
      top: 0;
      z-index: 10;
    }

    .logo {
      font-size: 1.6rem;
      font-weight: 700;
      color: #fff;
      display: flex;
      align-items: center;
      gap: 8px;
      letter-spacing: 0.5px;
    }

    .logo span {
      font-size: 1.7rem;
    }

    nav a {
      color: #fff;
      text-decoration: none;
      margin: 0 14px;
      font-weight: 500;
      transition: color 0.3s ease;
    }

    nav a:hover {
      color: #ffd166;
    }

    .btn-add {
      background: #ffb703;
      color: #fff;
      border: none;
      padding: 10px 18px;
      border-radius: 8px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .btn-add:hover {
      background: #f48c06;
      transform: translateY(-2px);
      box-shadow: 0 4px 10px rgba(244, 140, 6, 0.3);
    }

    /* Form Section */
    .form-section {
      flex: 1;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 80px 20px;
    }

    .form-box {
      background: #ffffff;
      border-radius: 20px;
      box-shadow: 0 15px 40px rgba(0,0,0,0.08);
      padding: 45px 40px;
      max-width: 550px;
      width: 100%;
      position: relative;
      overflow: hidden;
    }

    /* Decorative Shelf Shadow */
    .form-box::before {
      content: "";
      position: absolute;
      bottom: -8px;
      left: 20px;
      right: 20px;
      height: 8px;
      background: rgba(0,0,0,0.1);
      border-radius: 0 0 20px 20px;
      filter: blur(2px);
    }

    .form-box h1 {
      text-align: center;
      margin-bottom: 30px;
      color: #2d6a4f;
      font-size: 2rem;
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 10px;
    }

    .form-box h1 span {
      color: #ffb703;
      font-size: 1.7rem;
    }

    label {
      display: block;
      font-weight: 600;
      margin-bottom: 6px;
      color: #374151;
    }

    input, select {
      width: 100%;
      padding: 12px 14px;
      margin-bottom: 20px;
      border-radius: 10px;
      border: 1px solid #ccc;
      background: #fafafa;
      font-size: 15px;
      transition: all 0.3s ease;
    }

    input:focus, select:focus {
      border-color: #2d6a4f;
      box-shadow: 0 0 0 3px rgba(45, 106, 79, 0.2);
      outline: none;
      background: #fff;
    }

    button[type="submit"] {
      width: 100%;
      background: #2d6a4f;
      color: #fff;
      border: none;
      padding: 14px;
      font-size: 1rem;
      border-radius: 10px;
      font-weight: 600;
      cursor: pointer;
      transition: background 0.3s ease, transform 0.2s ease;
    }

    button[type="submit"]:hover {
      background: #238a5a;
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(35, 138, 90, 0.3);
    }

    footer {
      text-align: center;
      padding: 20px;
      font-size: 0.9rem;
      color: #666;
      background: #f0f0f0;
      border-top: 1px solid #ddd;
    }

    @media (max-width: 600px) {
      header {
        flex-direction: column;
        gap: 10px;
        padding: 20px;
      }
      header nav {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 10px;
      }
      .form-box {
        padding: 30px 25px;
      }
    }
  </style>
</head>
<body>

  <header>
    <div class="logo"><span>📖</span> Book Haven</div>
    <nav>
      <a href="/">Home</a>
      <a href="/add">Add Book</a>
      <a href="/books">Browse</a>
    </nav>
    <button class="btn-add" onclick="location.href='/add'">+ Add Book</button>
  </header>

  <section class="form-section">
    <div class="form-box">
      <h1><span>📚</span> Add New Book</h1>
      <form method="post" action="/add-book">
        <label>Book No.</label>
        <input type="text" name="bookNo" placeholder="Enter book number" required>

        <label>Book Name</label>
        <input type="text" name="bookName" placeholder="Enter book name" required>

        <label>Author</label>
        <input type="text" name="author" placeholder="Enter author’s name" required>

        <label>Language</label>
        <input type="text" name="language" placeholder="Enter language" required>

        <label>Published Date</label>
        <input type="date" name="publishedDate" required>

        <label>Duplicates</label>
        <input type="number" name="duplicates" placeholder="Number of copies" required>

        <label>Image URL</label>
        <input type="url" name="imageUrl" placeholder="Paste book cover image link">

        <button type="submit">Add Book</button>
      </form>
    </div>
  </section>

  <footer>
    © 2025 Book Haven — Your Gateway to Great Reads
  </footer>

</body>
</html>
